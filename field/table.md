# c2c_msg_table 字段解析总览

> 数据库：`nt_msg_plain.db` | 表：`c2c_msg_table` | 总行数：1,318,588
>
> 置信度说明：✅ 高（已验证）｜ 🔍 中等推测 ｜ ❓ 低置信 / 未知

| 字段 | 类型 | 置信度 | 简称 | 概述 | 详细报告 |
|------|------|--------|------|------|----------|
| `40001` | INTEGER (PK) | ✅ 高 | **消息唯一 ID** | 表主键，131 万行零重复；全局唯一，近似时间递增（94.4% 单调）；非 Snowflake 结构，推测 QQ 自研 64 位 ID | [40001.md](40001.md) |
| `40002` | INTEGER | ✅ 高 | **消息随机数** | 客户端随机生成的 32-bit 整数（msgRandom），用于服务端去重 / ACK 匹配；不重复率 98.84%，重复属生日碰撞；与时间无单调关系；NT 新协议少数值扩展至 64-bit | [40002.md](40002.md) |
| `40003` | INTEGER | ✅ 高 | **全局倒序序号** | 全局唯一序号命名空间，值越大越旧（按时间 100% 单调递减）；大量会话固定为 0，不适合用作唯一标识 | [40003.md](40003.md) |
| `40005` | INTEGER | ✅ 高 | **clientSeq** | 客户端 16 位消息序号（uint16_t），收到时存对方序号（严格 +1），发出时存己方序号；值域精确为 [0, 65535]，会话间共用数值空间 | [40005.md](40005.md) |
| `40006` | INTEGER | ✅ 高 | **serverSeq** | 服务端操作序号（64 位复合 ID，高位 `0x01` 前缀 + 低 32 位序号）；92.1% 为 `0`（旧协议历史数据）；非零值与时间强正相关（99.8% 单调）；与 40001 强负相关（-0.96）；同一批量操作共享同一值 | [40006.md](40006.md) |
| `40008` | INTEGER | ❓ 低 | **协议保留字段** | 2025-11 前全 NULL；2025-12 起恒为 `0`，与 40009 完全同步；NT 新协议预留字段，无语义区分 | [40008.md](40008.md) |
| `40009` | INTEGER | 🔍 中 | **批量转发标志** | 2025-11 前全 NULL；`0`=普通单发（99.96%），`1`=同批次多目标转发（14 条，发送者本人，40013∈{4,5}，多为合并转发消息包） | [40009.md](40009.md) |
| `40010` | INTEGER | ❓ 低 | **消息协议格式标志** | 99.97% 恒为 `1`（标准格式）；`103`/`114` 全为接收方向且 40800=NULL（疑似控制通知）；`100` 集中于特定会话有消息体（疑似特殊协议）；语义无直接验证 | [40010.md](40010.md) |
| `40011` | INTEGER | 🔍 中 | **消息内容类型** | 类 MIME type；`2`=标准消息（99.3%），`3`=文件，`6`=名片，`7`=位置，`8`=语音，`9`=Reaction，`11`=合并转发，`17`=状态通知等 | [40011.md](40011.md) |
| `40012` | INTEGER | 🔍 中 | **消息属性标志位** | 在 `40011=2` 时为真正 bitmask：bit0=基础，bit1=富媒体，bit4=撤回，bit12=长消息；其他 `40011` 值下固定为特定常量 | [40012.md](40012.md) |
| `40013` | INTEGER | ✅ 高 | **消息方向** | `0`=收到，`1`=发出，`2`=发出（特殊类型），`3`=系统消息，`4/5`=极少量特殊状态；与 40030/40033 联动验证吻合率 >99.8% | [40013.md](40013.md) |
| `40020` | TEXT | ✅ 高 | **发送者 NT UID** | `u_` 前缀 24 字符字符串；与 40033（发送者 QQ）严格一一对应；发送消息时为 owner UID，接收消息时为对方 UID | [40020.md](40020.md) |
| `40021` | TEXT | ✅ 高 | **会话对象 NT UID** | 始终为 40030（会话对象 QQ）对应的 NT UID，与方向无关；≠ 接收者字段 | [40021.md](40021.md) |
| `40026` | INTEGER | ❓ 低 | **未知** | 全表 131 万行恒为 `0`，无法通过统计分析推断语义 | — |
| `40027` | INTEGER | ✅ 高 | **本地会话 ID** | 客户端为每个私聊联系人分配的本地递增整数编号（1~561，共 560 个唯一值）；与 40030 严格一一对应；全表 5 个索引均以此字段为前导列，直接验证其会话分区键地位 | [40027.md](40027.md) |
| `40030` | INTEGER | ✅ 高 | **私聊会话对象 QQ** | 本次会话对方的 QQ 号，与消息方向无关；共 561 个唯一值，与 40027 一一对应 | [40030.md](40030.md) |
| `40033` | INTEGER | ✅ 高 | **发送者 QQ** | 实际发送该消息的 QQ 号；40013=0 时为对方 QQ，40013=1/2 时为本人 QQ | [40033.md](40033.md) |
| `40040` | INTEGER | ❓ 低 | **NT 协议同步标志** | 疑似多端同步标志：`0`=本机自然产生，`1`=NT 服务端同步下发；2024-11 后出现；已读假说已排除 | [40040.md](40040.md) |
| `40041` | INTEGER | ❓ 低 | **未知** | 99.9996% 恒为 `2`；仅 5 行为 `0`（均为 40040=1 的 NT 同步消息），无法通过统计推断语义 | — |
| `40050` | INTEGER | ✅ 高 | **发送时间戳** | Unix 秒级时间戳，消息发送时间；max=2026-05-12，与 40058 一致性验证通过 | — |
| `40052` | INTEGER | ❓ 低 | **未知** | 全表 131 万行 100% 为 `0`，无法推断语义 | — |
| `40058` | INTEGER | ✅ 高 | **日期戳** | 当日 00:00:00 的 Unix 时间戳（UTC+8 日期索引） | — |
| `40060` | INTEGER | ❓ 低 | **NT 协议版本标志** | 疑似协议版本标志：`1`=旧协议（2008–2024），`0`=NT 新协议（2024-10 起）；2025 年起收到消息全为 `0`；与 40040 绝不同时为 `1` | [40060.md](40060.md) |
| `40062` | BLOB | ❓ 低 | **扩展 BLOB C** | 99.9984% 为 NULL；仅 21 行为固定 4 字节值 `\xf2\xc7\x13\x00`（小端序 0x0013C7F2），无法推断语义 | — |
| `40083` | INTEGER | ❓ 低 | **未知** | 样本全为 `0` | — |
| `40084` | INTEGER | ❓ 低 | **未知** | 样本全为 `0` | — |
| `40090` | TEXT | ❓ 低 | **扩展文本** | 样本全为空字符串，无法分析；可能存储引用摘要等附加文字 | — |
| `40093` | TEXT | ✅ 高 | **发送者昵称** | 发送者的 QQ 昵称或备注名 | — |
| `40100` | INTEGER | ❓ 低 | **未知** | 样本全为 `0` | — |
| `40105` | INTEGER | ❓ 低 | **已读/送达标志** | 仅发送方向（40013=1/2）可能为 `1`，接收消息恒为 `0`；推测为对方已读/已送达标志，语义无直接验证 | [40105.md](40105.md) |
| `40600` | BLOB | ❓ 低 | **固定魔数/版本标记** | 92.1% 为 NULL；非NULL中 99.5% 为固定 8 字节常量 `C2E91304A8D11400`（小端 `[0x0413e9c2, 0x0014d1a8]`），区分度极低；少量变长行（16/33/77 字节）可能为 protobuf 片段 | — |
| `40601` | BLOB | ❓ 低 | **特殊消息附加数据** | 99.98% 为 NULL；非NULL仅 208 条（长度 31~99 字节），为 protobuf 结构，含 base64 token 及可读字符串（如 "QQ邮箱提醒"），仅出现于特殊系统推送消息 | — |
| `40605` | BLOB | ❓ 低 | **固定魔数（稀少）** | 99.99% 为 NULL；非NULL仅 99 条，全为固定 4 字节 `EAE91300`（小端 `0x0013E9EA`），无区分度 | — |
| `40800` | BLOB | ✅ 高 | **消息体** | Protobuf 编码的实际消息内容；内层按 `40011` 类型分区，已逆向还原 ~150 个字段（含文本/图片/CDN 元数据/位置/语音/通话/合并转发/引用回复等） | [40800.md](40800.md) |
| `40801` | BLOB | ❓ 低 | **消息摘要** | 90.1% 为 NULL；非NULL（130,693 条）与 40800 严格同步出现；长度 37~140 字节，首字节恒为 `0x8A`（protobuf field 17 LEN），疑为消息推送通知用精简 protobuf 副本 | — |
| `40850` | INTEGER | ✅ 高 | **被反应消息 clientSeq** | 仅 Reaction 消息（40011=9）非零；值等于被 Reaction 的原消息的 clientSeq（40005）；clientSeq 匹配率 99.6%（1,792 行验证） | [40850.md](40850.md) |
| `40851` | INTEGER | 🔍 中 | **Reaction 发送时间戳** | 仅 Reaction 消息（40011=9）非零；存储 Reaction 发出时的 Unix 时间戳（秒），值域 2024-07 ~ 2026-05，与 40850 几乎完全共现（99.9%） | [40851.md](40851.md) |
| `40900` | BLOB | 🔍 中 | **引用消息快照** | 仅 reaction/引用回复等消息类型非 NULL；BLOB 为嵌套 protobuf，子消息字段体系与主表相同；内含被引用消息的 NT UID、MD5、UUID 等元数据 | [40900.md](40900.md) |

---

## 已分析字段汇总

| 字段 | 简称 | 置信度 |
|------|------|--------|
| `40001` | 消息唯一 ID | ✅ 高 |
| `40002` | 消息随机数（msgRandom） | ✅ 高 |
| `40003` | 全局倒序序号 | ✅ 高 |
| `40005` | clientSeq（客户端消息序号） | ✅ 高 |
| `40006` | serverSeq（服务端操作序号） | ✅ 高 |
| `40008` | 协议保留字段 | ❓ 低 |
| `40009` | 批量转发标志 | 🔍 中 |
| `40010` | 消息协议格式标志 | ❓ 低 |
| `40011` | 消息内容类型 | 🔍 中 |
| `40012` | 消息属性标志位 | 🔍 中 |
| `40013` | 消息方向 | ✅ 高 |
| `40020` | 发送者 NT UID | ✅ 高 |
| `40021` | 会话对象 NT UID | ✅ 高 |
| `40027` | 本地会话 ID | 🔍 中 |
| `40030` | 私聊会话对象 QQ | ✅ 高 |
| `40033` | 发送者 QQ | ✅ 高 |
| `40040` | NT 协议同步标志 | ❓ 低 |
| `40050` | 发送时间戳 | ✅ 高 |
| `40058` | 日期戳 | ✅ 高 |
| `40060` | NT 协议版本标志 | ❓ 低 |
| `40093` | 发送者昵称 | ✅ 高 |
| `40105` | 已读/送达标志 | ❓ 低 |
| `40800` | 消息体（Protobuf） | ✅ 高 |
| `40850` | 被反应消息 clientSeq | ✅ 高 |
| `40851` | Reaction 发送时间戳 | 🔍 中 |
| `40900` | 引用消息快照 | 🔍 中 |
