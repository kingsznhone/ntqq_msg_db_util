# 字段 `40800` — 消息体（Protobuf）

## 字段属性

| 属性 | 值 |
|------|-----|
| 字段编号 | 40800 |
| 数据类型 | BLOB |
| 是否主键 | 否 |
| 置信度 | ✅ 高 |

## 含义说明

`40800` 存储消息的**实际内容主体**，编码为 Protobuf（wire format）。
本列是整张表最核心的字段，文本、图片、文件、语音、位置、合并转发、通话记录等所有消息内容均序列化于此。

- 全表 1,318,588 行中，1,318,117 行非 NULL（约 99.96%）
- NULL 仅出现于部分 `40011=0/1` 的无效/已删除消息中

---

## Protobuf 结构（逆向还原）

### 外层包装：`MsgBody`

```
message MsgBody {
  repeated MsgContent content = 40800;
}
```

- 顶层只有 `field 40800`（wire key `40800-2`）
- **1,240,057** 行解码为单个 `dict`（content=1 条），**78,060** 行为 `list`（content≥2 条，repeated）
- 另有 **28** 行以 `wire key 40800-1`（varint encoding）出现，推测为旧协议兼容字段

#### 多段消息（图文混排，78,060 行）

原始报告将此部分标注为"内层解码失败"，实为**误报**。全量验证结果：78,060 行中的 list item 全部是已解码的 `dict`（MsgContent），**0 条 bytes item，0 条解码失败**。真实含义是：该行消息由多个 `MsgContent` 段组成，proto repeated 语义。

**段数分布**：

| 段数 | 行数 | 说明 |
|------|------|------|
| 2 | 73,115 (93.7%) | 绝大多数为二段 |
| 3 | 3,524 | 三段 |
| 4–9 | 1,123 | 少量多段 |
| 10+ | 298 | 极少数（最多 101 段） |

**最常见的段类型组合**（按 45002 值）：

| 排名 | 行数 | 组合 |
|------|------|------|
| 1 | 51,764 | 图片/媒体 + 文字（先图后文） |
| 2 | 9,868 | 名片 + 文字（分享联系人附言） |
| 3 | 2,771 | 文字 + 图片/媒体（先文后图） |
| 4 | 2,736 | 文字 + 名片 |
| 5 | 2,541 | 位置 + 文字（分享位置附言） |
| 6 | 2,501 | 文字 + 文字（文本拆段，含表情换行） |

**45001 连续性**：相邻两段的 45001 差值在 88,478 对中有 98.0%（86,693 对）≤ 2，确认 45001 为全局单调递增 ID，同一消息的各段共享连续 ID 区段。

### 内层主消息：`MsgContent`

`MsgContent` 字段按功能分区，按 `40011` 消息类型激活不同字段组：

| 字段区段 | 字段号范围 | 适用 40011 |
|---------|-----------|-----------|
| 通用头部 | 45001–45004 | 所有 |
| 文本 | 45101–45112 | 2（文本） |
| 媒体基础 | 45402–45419 | 1,2,3,5,6,7 |
| 媒体 CDN 元数据 | 45501–45550 | 1,2,3,5,6,7 |
| 图片/视频 CDN 资源 | 45800–45829 | 2（图片/视频） |
| 短视频位置 tag | 45851–45863 | 7（短视频，非地图 pin） |
| 名片 | 45906–45926 | 6 |
| 文件扩展 | 45954, 45974 | 1,3 |
| 稀有系统 | 47402–47419 | 0（极少） |
| 名片/引用 | 47501–47502, 47702–47715 | 5 |
| 视频附加 | 47601–47622 | 2（视频） |
| 合并转发 | 47901–47904 | 11 |
| 通话通知 | 48151–48157 | 19 |
| 引用回复扩展 | 48210–48275 | 5 |
| 未知类型 10 | 48401–48421 | 10 |
| 旧协议合并转发（XML） | 48601–48603 | 8 |
| 通用扩展 | 49154–49155 | 1,2,3,5,7,8,11,17 |
| 系统通知 | 80810–80995 | 17 |

---

## 已确认字段详解

### 通用头部（所有消息类型）

| 字段号 | 类型 | 出现次数 | 含义 | 置信度 |
|--------|------|----------|------|--------|
| `45001` | int64 | 1,240,057 | **消息唯一 ID**，与主表 `40001` 空间对应 | ✅ 高 |
| `45002` | int32 | 1,240,057 | **内容子类型**：`1`=文本，`2`=图片/媒体 | ✅ 高 |
| `45003` | int32 | 232,830 | 媒体子标志（出现于 40011=1,2,3,5,6,7） | 🔍 中 |
| `45004` | sub-msg | 5,145 | 空 sub-message，用途不明（仅 40011=2） | ❓ 低 |
| `40010` | int32 | 25,212 | 恒为 `1`，出现于多种类型，协议版本标志 | ❓ 低 |

### 文本字段（40011=2，content_type=1）

| 字段号 | 类型 | 出现次数 | 含义 | 置信度 |
|--------|------|----------|------|--------|
| `45101` | bytes | 992,666 | **消息正文**（纯 UTF-8 明文字符串）；极少数（约 0.4%）被 blackboxprotobuf 误解析为 dict（因 UTF-8 字节偶发符合 varint 格式），实为误报，取原始 bytes 仍可正常 decode | ✅ 高 |
| `45102` | int64 | 99,859 | 文本属性标志（多为 `0`） | 🔍 中 |
| `45112` | sub-msg | 240 | 编辑/已读时间戳：含 `50202`（Unix 秒）和 `50205`（恒为 2） | 🔍 中 |
| `45416` | int32 | 23,212 | 值 `1000`/`2000`，疑为图片质量或 DPI | ❓ 低 |
| `45103`–`45111` | int32/sub | 18,993 | 全部恒为 `0` 或空 sub-message，协议保留 | ❓ 低 |

### 媒体文件基础（40011=1,2,3,5,6,7）

| 字段号 | 类型 | 出现次数 | 含义 | 置信度 |
|--------|------|----------|------|--------|
| `45402` | bytes | 234,791 | **文件名**（MD5hex + 扩展名，如 `4672347F…C.jpg`） | ✅ 高 |
| `45405` | int64 | 233,982 | **文件大小**（字节） | ✅ 高 |
| `45406` | bytes | 234,638 | **MD5 二进制**（16 字节 raw binary） | 🔍 高 |
| `45408` | bytes | 21,980 | 缩略图/校验 hash（20 字节） | 🔍 中 |
| `45411` | int32 | 231,049 | **图片宽度**（像素） | ✅ 高 |
| `45412` | int32 | 231,049 | **图片高度**（像素） | ✅ 高 |
| `45419` | bytes | 212,714 | **文件扩展名**（如 `.png`、`.7z`） | ✅ 高 |
| `45403` | bytes | 2,283 | 本地发送路径（如 `D:\工作区\example_file.7z`） | ✅ 高 |
| `45407` | bytes | 351 | 16 字节 binary（第二 hash，用途不明） | ❓ 低 |
| `45409` | bytes | 116 | 20 字节 binary（类 45408） | ❓ 低 |
| `45415` | int32 | 994 | 时长或版本号（值域 ~1k–80k） | ❓ 低 |

### 媒体 CDN 元数据（45501–45550）

| 字段号 | 类型 | 出现次数 | 含义 | 置信度 |
|--------|------|----------|------|--------|
| `45503` | bytes | 234,791 | **CDN 下载 token**（base64 字符串） | 🔍 中 |
| `45504` | bytes | 216,333 | **CDN URL 路径**（如 `/XXXXXXXXXX-XXXXXXXX-…`） | 🔍 中 |
| `45505` | int64 | 22,150 | **CDN 过期时间戳**（Unix 秒） | 🔍 中 |
| `45515` | int64 | 994 | 短期过期时间戳（~7 天，与 `45516=604800` 配对） | 🔍 中 |
| `45516` | int32 | 994 | 短期 TTL 时长（`604800` = 7 天） | 🔍 中 |
| `45517` | int64 | 16,719 | **上传时间戳**（Unix 秒） | 🔍 中 |
| `45518` | int32 | 16,719 | 长期 TTL 时长（`2592000` = 30 天） | 🔍 中 |
| `45519` | int64 | 177 | 长期过期时间戳（~30 天后） | 🔍 中 |
| `45526` | sub-msg | 1 | 媒体引用子消息（含 MD5/UID/时间戳） | ❓ 低 |
| `45550` | int32 | 11,518 | 恒为 `4`，用途不明 | ❓ 低 |

### 图片/视频 CDN 资源（45800–45829）

| 字段号 | 类型 | 出现次数 | 含义 | 置信度 |
|--------|------|----------|------|--------|
| `45802` | bytes | 23,124 | **CDN 下载 URL 路径**（`/download?appid=1406&fileid=…`） | ✅ 高 |
| `45803` | bytes | 23,124 | CDN URL 路径（备用或第二端点，与 45802 同步出现） | 🔍 中 |
| `45804` | bytes | 23,124 | CDN URL 路径（第三端点） | 🔍 中 |
| `45812` | bytes | 3,727 | **本地缓存路径**（`C:\Users\<user>\Documents\Tencent Files\…`） | ✅ 高 |
| `45814` | bytes | 9 | NTOS 完整路径（`::NTOSFull::C:\…`） | 🔍 中 |
| `45815` | repeated bytes | 22,569 | **文本降级标记**（如 `[动画表情]`），可重复 | ✅ 高 |
| `45816` | bytes | 21,652 | **CDN 主机名**（`multimedia.nt.qq.com.cn`，恒定） | ✅ 高 |

### 贴图/动画表情（45600，40011=2,7）

嵌套三层 sub-message，结构如下：

```
StickerInfo { f1 { f1 { sticker_id=int64, md5=bytes, extra=bytes } } }
```

- `sticker_id`：贴图数字 ID（如 `1942758`、`3027193`）
- `md5`：32 字节 hex MD5 字符串

---

### 位置消息（40011=7，45851–45863）

> ⚠️ **重要修正（2026-05-13 全量验证）**：
> 本数据库中 `40011=7` 的 876 条记录，经逐条解码确认，**全部为「带位置 tag 的短视频」**（`content_type=5`，`45402` 为视频文件路径），
> 而非「分享我的位置」地图 pin 消息。`45853`–`45855` 全量验证均为 `0`，**不存在经纬度数据**。
> 真正的地图 pin 消息（发送 GPS 坐标）在本数据集中**从未出现**，可能该用户从未使用过此功能，
> 或更新版协议将其归入 `40011=2`+`content_type=6`（富文本，735 条，尚未分析）。

#### `content_type`（45002）分布

| content_type | 含义 | 出现次数 |
|---|---|---|
| `5` | 带位置 tag 的短视频 | 全部 876 条 |

#### 字段说明

| 字段号 | 类型 | 出现次数 | 含义 | 置信度 |
|--------|------|----------|------|--------|
| `45851` | int32 | 699 | 值恒为 `2`，含义不明（视频子类型？） | ❓ 低 |
| `45852` | int32 | 699 | 值 `0` 或 `1` | ❓ 低 |
| `45853` | int32 | — | **全量 0**，非经纬度 | ❌ 排除坐标 |
| `45854` | int32 | 699 | 值 `0` 或 `1` | ❓ 低 |
| `45855` | int32 | — | **全量 0**，非经纬度 | ❌ 排除坐标 |
| `45856` | sub-msg | 699 | 子消息（含 field 7 和 field 11，均为 int32=0） | ❓ 低 |
| `45862` | bytes | 876 | 16 字节 binary hash（100% UTF-8 失败） | ❓ 低 |
| `45863` | int32 | 184 | 值 `0` 或 `1` | ❓ 低 |

---

### 名片分享（40011=6，45906–45926）

| 字段号 | 类型 | 出现次数 | 含义 | 置信度 |
|--------|------|----------|------|--------|
| `45906` | repeated int32 | 37 | 名片类别（值如 `11`、`7`、`16`） | ❓ 低 |
| `45923` | bytes | 2 | 名片附加文本（UTF-8） | 🔍 中 |
| `45925` | bytes | 930 | 头像数据（binary） | 🔍 中 |

---

### 名片/引用回复（40011=5，477xx）

| 字段号 | 类型 | 出现次数 | 含义 | 置信度 |
|--------|------|----------|------|--------|
| `47501` | int32 | 7 | 引用类型，恒为 `1` | 🔍 中 |
| `47703` | bytes | 388 | 名片目标 NT UID（`u_...` 字符串） | ✅ 高 |
| `47704` | bytes | 388 | 同 47703（可能为发送者 UID） | 🔍 中 |
| `47705` | bytes | 385 | 昵称（UTF-8） | ✅ 高 |
| `47706` | bytes | 143 | 备注名（UTF-8） | 🔍 中 |
| `47710` | sub-msg | 193 | **被引用消息内容（递归嵌套完整 MsgContent）** | ✅ 高 |
| `47713` | bytes | 388 | 引用的文字摘要（UTF-8，或空 sub-msg） | 🔍 中 |

`47710` 是最重要的发现：**引用消息的完整消息体以递归方式内嵌**，字段体系与外层 `MsgContent` 完全相同。

---

### 合并转发（40011=11，479xx）

| 字段号 | 类型 | 出现次数 | 含义 | 置信度 |
|--------|------|----------|------|--------|
| `47901` | bytes | 640 | **JSON 元数据**（`{"app":"com.tencent.structmsg","ver":"1.0.0.19","prompt":"[QQ聊天记录]",...}`） | ✅ 高 |
| `47902` | bytes | 55 | base64 下载 token | 🔍 中 |
| `47904` | bytes | 55 | **合并转发包 UUID**（如 `e1ef654c-4ca7-4d89-9ceb-c0ae1962…`） | ✅ 高 |

---

### 通话通知（40011=19，481xx）

| 字段号 | 类型 | 出现次数 | 含义 | 置信度 |
|--------|------|----------|------|--------|
| `48151` | int32 | 32 | 通话类型（`33`=远程协助，`7`=普通通话，`13`=？） | 🔍 中 |
| `48152` | int32 | 32 | 通话时长或状态码 | ❓ 低 |
| `48153` | bytes | 32 | **通话说明文本**（UTF-8，如 `通话时长 00:01`、`已在其他设备处理`） | ✅ 高 |

---

### 旧协议合并转发（40011=8，486xx）

> ⚠️ **修正（2026-05-13 实测）**：`40011=8` 并非语音消息，而是**旧协议合并转发**（content_type=16）。
> 字段内容为 XML 格式聊天记录，与 `40011=11`（NT 协议合并转发，JSON/proto）并列，共 9,440 行。

| 字段号 | 类型 | 出现次数 | 含义 | 置信度 |
|--------|------|----------|------|--------|
| `48601` | string | 492 | **合并转发 resid**（base64 下载 token，即 XML 中的 `m_resid` 值） | ✅ 高 |
| `48602` | string | 493 | **合并转发 XML 描述**（`action="viewMultiMsg"` 格式，含标题/摘要/消息预览） | ✅ 高 |
| `48603` | string | 486 | **合并转发包 UUID**（与 XML 中 `m_fileName` 一致，如 `79A96F32-DB31-…`） | ✅ 高 |

---

### 系统通知（40011=17，80810–80995）

| 字段号 | 类型 | 出现次数 | 含义 | 置信度 |
|--------|------|----------|------|--------|
| `80810` | int32 | 417 | 通知子类型 | ❓ 低 |
| `80900` | bytes | 417 | 系统通知内容（binary，可能为嵌套 proto） | 🔍 中 |
| `80970` | repeated sub | 213 | repeated sub-message，用途不明 | ❓ 低 |

---

### 通用扩展（多类型，49154–49155）

| 字段号 | 类型 | 出现次数 | 适用 40011 | 含义 | 置信度 |
|--------|------|----------|-----------|------|--------|
| `49154` | string | 29,993 | 2,3,5,7,8,9,11,17 | **NT 客户端协议版本标识**：仅 2 个枚举值，随客户端版本切换 | ✅ 高 |
| `49155` | int64 | 27,598 | 同上 | **消息内层时间戳**（Unix 秒），与外层 40050 高度一致（±5s 内占 99.7%） | ✅ 高 |

#### 49154 枚举值详解

全库仅出现 **2 个枚举值**，100% 为可打印 ASCII，全量 UTF-8 解码无失败：

| 值 | 长度 | 出现次数 | 时间范围 | 含义 |
|----|------|---------|---------|------|
| `"1"` | 1 字节 | 19,956 | 2024-11 ~ 2025-11 | NT 早期客户端版本标识 |
| `"nt_1"` | 4 字节 | 10,037 | 2025-11 起（2025-12 完全切换）| NT 更新版客户端版本标识 |

**关键验证**：
- **100%** 出现在 `40060=0`（NT 新协议）消息中，旧协议（40060=1）中从不出现
- **97–98%** 出现在 `40013=1`（发出）消息中（少量接收消息中出现推测为服务端回写）
- **时间切换**：2025-11 为过渡期，2025-12 起 `"1"` 完全消失、仅剩 `"nt_1"`
- 同一消息的多段 `MsgContent` 中，49154 值**恒相同**（2,387 条多段消息验证，0 例不一致）

#### 49155 时间戳详解

| 项目 | 数值 |
|------|------|
| 出现次数 | 27,598（与 49154 同行的子集）|
| 唯一值数 / 唯一率 | 27,532 / **99.76%** |
| 与 40050 差值 \|diff\| < 5s | **99.7%** |
| 平均差（49155 − 40050） | −2.3s（客户端本地时间略早于服务端记录）|
| 差值范围 | −26,629s ~ 0s |

---

## Proto 定义（当前版本）

完整 proto 定义详见仓库根目录 [`msg.proto`](../msg.proto)。

核心结构摘要：

```protobuf
syntax = "proto3";
package qq;

message MsgBody {
  repeated MsgContent content = 40800;
}

message MsgContent {
  // 通用头部
  int64  msg_id       = 45001;
  int32  content_type = 45002;
  int32  media_sub    = 45003;

  // 文本
  bytes  text         = 45101;
  int64  text_attr    = 45102;

  // 媒体文件
  bytes  filename     = 45402;
  int64  filesize     = 45405;
  bytes  md5_raw      = 45406;
  int32  img_width    = 45411;
  int32  img_height   = 45412;
  bytes  file_ext     = 45419;

  // CDN
  bytes  cdn_token    = 45503;
  bytes  cdn_path     = 45504;
  bytes  cdn_url_1    = 45802;
  bytes  cdn_host     = 45816;
  repeated bytes text_fallback = 45815;
  bytes  local_path   = 45812;

  // 引用（递归）
  MsgContent ref_msg  = 47710;

  // 合并转发
  bytes  fwd_meta     = 47901;
  bytes  fwd_uuid     = 47904;

  // 通话
  bytes  call_desc    = 48153;

  // … 完整定义见 msg.proto
}
```

---

## 关联字段

| 字段 | 关系 |
|------|------|
| `40011` | 消息类型，决定 `40800` 内激活哪些字段区段 |
| `40012` | 消息属性标志位，与 `MsgContent.content_type`（45002）对应 |
| `40850` | 被 Reaction 的原消息 clientSeq，指向的消息其 40800 含有 45001 等字段 |
| `40900` | 引用消息快照，其 BLOB 内字段编号体系与 40800 内层完全一致 |

---

## 未解决问题

1. **45105/45110/45818–45828**：一批空 sub-message，可能在某些条件下携带内容
2. **48210–48275**：40011=5 的引用回复扩展区段，语义未确认
3. **45004 / 47408 / 47412 等空 sub-message**：存在但无内容，可能为扩展占位


### bytes 字段类型分析（全量 UTF-8 验证，1,318,117 行）

对所有 `bytes` 字段的值执行 `.decode('utf-8')` 验证，结果分三类：

**真正的 binary 字段（不可改 `string`）**：

| 字段号 | 出现次数 | 失败率 | 说明 |
|--------|----------|--------|------|
| `45406` | 162,781 | 100% | 16 字节 raw binary MD5 |
| `45408` | 24,564 | 100% | 20 字节 binary hash |
| `45409` | 117 | 100% | 20 字节 binary hash（同 45408） |
| `45407` | 302 | 100% | 16 字节 binary（第二 hash） |
| `45421` | 176 | 100% | 20 字节 binary hash |
| `45862` | 862 | 100% | 16 字节 binary hash |
| `80903` | 424 | 100% | 系统通知 body（嵌套 proto binary） |

**混合字段（偶有 binary，保留 `bytes`）**：

| 字段号 | 出现次数 | 失败率 | 说明 |
|--------|----------|--------|------|
| `45424` | 14,511 | 5.8% | 通常为 16 字节 MD5 raw，少数为文本 |
| `45925` | 928 | 3.8% | 头像数据（主体为图片 binary） |

**100% UTF-8 字符串字段（共 77 个）**：包含 `45101`、`45402`、`45403`、`45419`、`45503`、`45504`、`45802`–`45804`、`45812`–`45816`、`47703`–`47706`、`47713`–`47715`、`47901`、`47902`、`47904`、`48153`、`48601`–`48603`、**`49154`**（枚举值 `"1"` / `"nt_1"`）等所有文本类字段。
