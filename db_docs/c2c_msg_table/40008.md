# 字段 `40008` — 协议保留字段

| 属性 | 值 |
|---|---|
| 字段编号 | `40008` |
| 类型 | INTEGER |
| 置信度 | ❓ 低 |

## 结论

当前只能确认：历史记录为 NULL；NT 新协议时期写入的非 NULL 值全部为 `0`。该字段没有可区分的业务信息，因此按协议保留字段处理。

## 统计

- 非 NULL：`39,094` 行；
- NULL：`1,279,494` 行；
- 非 NULL 唯一值：`0`；
- 与 `40009` 的 NULL/非 NULL 填充模式完全同步。

## 使用边界

读取程序应保留原值，但不应根据 `40008=0` 生成业务判断。后续若出现非零值，再重新分析其与 `40009` 的关系。
