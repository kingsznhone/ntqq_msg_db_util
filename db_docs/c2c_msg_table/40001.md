# 字段 `40001` — 消息唯一 ID

| 属性 | 值 |
|---|---|
| 字段编号 | `40001` |
| 类型 | INTEGER |
| 主键 | 是 |
| 置信度 | ✅ 高 |

## 结论

`40001` 是 `c2c_msg_table` 的消息表主键和全局唯一消息 ID。当前快照 `1,318,588` 行无重复。

## 关键统计

| 特征 | 结果 |
|---|---|
| 值域 | `7436569264759096330`–`7639058395275417231` |
| 与 `40050` 的单调关系 | 按 `40001` 升序时，`40050` 单调不减约 `94.4%` |
| Snowflake 结构 | 不符合标准 Snowflake 时间位布局 |
| 与 `40003` | Pearson 约 `-0.43`，生成逻辑不同 |

## 使用边界

- 用 `40001` 唯一定位消息，不要用 `40002`、`40003` 或 `45001` 替代。
- 数值大致随时间递增，但不保证严格时间排序。
