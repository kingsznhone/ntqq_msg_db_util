# group_msg_table 字段解析总览

> 研究对象：`nt_msg_plain.db` 的 `group_msg_table`。本页先固定字段清单和统计入口；字段含义、置信度将在逐字段验证后补充。
>
> 当前数据集总行数：`540,018`。报告生成时间：`2026-07-20`。

## 字段总览

| 字段 | 类型 | 置信度 | 简称/待验证假设 | 概述 | 详细报告 |
|------|------|--------|-----------------|------|----------|
| `40001` | `INTEGER` | ✅ 高 | **消息唯一 ID** | 表主键，全表无重复；随发送时间近似递增，但不是时间戳 | [40001.md](40001.md) |
| `40002` | `INTEGER` | ✅ 高 | **消息随机数** | 高基数、低位近似均匀、与时间无关；存在少量碰撞，不是唯一键 | [40002.md](40002.md) |
| `40003` | `INTEGER` | ✅ 高 | **群内消息序号** | 同一群内随 `40050` 非递减；序号可跨群复用，不是全局唯一值 | [40003.md](40003.md) |
| `40005` | `INTEGER` | 🔍 中 | **发送侧序号/clientSeq 候选** | 99.76% 为 `0`；非零值中 99.46% 属于 `40013=1`，同一会话连续非零样本 98.06% 递增、95.66% 以 `+1` 变化；与 C2C 同编号 `clientSeq` 特征一致，但 group 的填充条件更窄 | — |
| `40006` | `INTEGER` | ❓ 低 | **服务端复合 ID 候选** | 绝大多数非零值具有固定高 32 bit 前缀、低 32 bit 高度离散，且无明显群内时间单调性；可能是 serverSeq/操作 ID，具体语义未确认 | — |
| `40008` | `INTEGER` | ❓ 低 | **会话内序号/操作序号候选** | distinct `438,652`；会话内按 `40050` 约 99.99% 非递减，但具体是序号、操作 ID 还是其他协议标识尚未确认 | — |
| `40009` | `INTEGER` | ❓ 低 | **消息处理/存储路径标志候选** | `1` 覆盖 99.30% 消息，`0` 集中于系统/特殊消息；具体是否为批量转发或同步标志尚未确认 | — |
| `40010` | `INTEGER` | 🔍 中 | **聊天类型** | 当前群表全为 `2`，与群聊类型假设一致；完整枚举需跨 c2c/频道等表继续验证 | — |
| `40011` | `INTEGER` | ✅ 高 | **消息类型** | 群消息外层类型；与 `40800` protobuf 内部结构高度对应 | [40011.md](40011.md) |
| `40012` | `INTEGER` | ✅ 高 | **消息子类型/属性组合** | `40011=2` 时表现为文本、富媒体、撤回/状态、长消息等属性位组合；其他 `40011` 下多为类型专属编码或固定组合 | [40012.md](40012.md) |
| `40013` | `INTEGER` | ✅ 高 | **发送/来源标志** | `0`=收到他人消息，`1`=本人正常发送，`2`=本人发送特殊类型，`3`=系统消息，`4/5`=极少量特殊状态 | [40013.md](40013.md) |
| `40020` | `TEXT` | ✅ 高 | **发送者 NT UID** | 实际发送者 NT UID；标准格式为 `u_` 加 22 个 Base64url 字符，与 `40033` 保持一对一映射 | [40020.md](40020.md) |
| `40021` | `TEXT` | ✅ 高 | **群聊对象 ID** | 以十进制群号字符串存储；与 `40030` 数值内容 100% 对应，与 `40027` 稳定对应 | [40021.md](40021.md) |
| `40026` | `INTEGER` | ❓ 低 | **未知常量字段** | 全表 `540,018` 行均为 `0`，不随消息类型、方向、时间、群号或消息体变化，无法推断具体语义 | — |
| `40027` | `INTEGER` | ✅ 高 | **本地群聊会话键** | 客户端本地会话分区 ID；与 `40021`、非零 `40030` 稳定对应，并分区 `40003` 群内序号 | [40027.md](40027.md) |
| `40030` | `INTEGER` | ✅ 高 | **群号** | 非零值标识群聊；与 `40027` 稳定对应，`0` 主要用于系统/特殊记录 | [40030.md](40030.md) |
| `40033` | `INTEGER` | ✅ 高 | **发送者 QQ** | 实际发送该群消息的 QQ 号；同一群可有多个发送者，系统消息通常为 `0` | [40033.md](40033.md) |
| `40040` | `INTEGER` | 🔍 中 | **NT 同步/消息来源流程标志** | `0` 为普通默认路径，`1` 高度集中于本人发送和特殊 NT 路径；是否严格表示多端同步仍待验证 | [40040.md](40040.md) |
| `40041` | `INTEGER` | 🔍 中 | **消息发送/处理状态** | `2` 为绝大多数正常/已处理记录；`0/3` 仅在特殊发送记录中出现，具体枚举待验证 | [40041.md](40041.md) |
| `40050` | `INTEGER` | ✅ 高 | **消息发送时间戳** | Unix 秒级时间戳；按 UTC+8 解释，与 `40058` 日期索引逐行一致 | [40050.md](40050.md) |
| `40052` | `INTEGER` | ❓ 低 | **未知常量字段** | 全表 `540,018` 行均为 `0`，不随消息类型、方向、时间、群号或消息体变化 | — |
| `40058` | `INTEGER` | ✅ 高 | **日期索引** | UTC+8 当日 00:00:00 的 Unix 秒级时间戳；与 `40050` 逐行精确一致 | [40058.md](40058.md) |
| `40060` | `INTEGER` | ❓ 低 | **协议/生命周期路径标志** | group 表全量恒为 `0`；跨表对照显示 c2c 存在 `0/1` 两种取值，但具体枚举语义不能直接移植到 group | — |
| `40062` | `BLOB` | 🔍 中 | **表态/Reaction 明细** | 结构化嵌套 protobuf；包含重复的表态条目明细，与 `40083/40084` 高度同步 | [40062.md](40062.md) |
| `40083` | `INTEGER` | 🔍 中 | **表态统计摘要候选** | 与 `40062`、`40084` 高度同步；具体统计维度尚未确认 | [40083.md](40083.md) |
| `40084` | `INTEGER` | 🔍 中 | **表态统计摘要候选** | 与 `40062`、`40083` 高度同步；具体统计维度尚未确认 | [40084.md](40084.md) |
| `40090` | `TEXT` | 🔍 中 | **用户自定义群昵称** | 与 `40093` 通常互斥，65.64% 非空；当前推断为用户设置的群名片/群昵称 | — |
| `40093` | `TEXT` | 🔍 中 | **默认昵称回退** | 与 `40090` 通常互斥，33.62% 非空；当前推断为未设置群昵称时使用的用户默认昵称 | — |
| `40100` | `INTEGER` | 🔍 中 | **@ / 回复提醒状态** | `0` 为默认无特殊状态；`6` 高度集中于回复消息，`2` 主要与回复/涉及他人 @ 相关；具体枚举仍需结合 protobuf 内容确认 | — |
| `40105` | `INTEGER` | ❓ 低 | **发送侧确认/回执候选** | 初步统计显示 `40105=1` 仅出现在本人发送相关方向 `40013=1/2`，但尚不能区分确认、回执或送达流程；保留初步探查结论，具体语义待验证 | — |
| `40600` | `BLOB` | ❓ 低 | **状态附加 Protobuf BLOB** | 全部非 NULL 数据可由 group 专用超集解码；主要包含 `42211`、`42261`，具体业务语义暂不确定 | [40600.md](40600.md) |
| `40601` | `BLOB` | ❓ 低 | **空字段/未发现数据** | 统计显示当前 `group_msg_table` 的 `540,018` 行全部为 NULL（非 NULL：`0`）；暂无原始样本可判断是否为 Protobuf、提取字段号或建立解码超集 | — |
| `40605` | `BLOB` | ❓ 低 | **空 Protobuf 附加字段候选** | 非 NULL 仅 `10` 行，全部是长度 `4` 的空 length-delimited Protobuf：顶层字段 `40605`、内部长度为 `0`；已建立 group 专用第一版超集并完成 `10/10` 强类型解析，但业务语义尚不明确 | — |
| `40800` | `BLOB` | ✅ 高 | **群消息体（Protobuf 超集）** | group 专用超集已覆盖 99.94% 非 NULL 消息；支持文本、媒体、回复、转发、系统和通话等结构，失败样本保留 wire fallback 研究 | [40800.md](40800.md) |
| `40801` | `BLOB` | ❓ 低 | **摘要/状态快照 Protobuf** | 非 NULL `536,004` 行全部可由 group 专用第一版超集强类型解析；外层为重复 `40801` message，绝大多数记录含 `2` 个内部条目，内部主要为 `41501/41502` 及 `415xx/416xx` 状态字段；结构和业务语义总体均保持低置信度 | — |
| `40850` | `INTEGER` | ❓ 低 | **历史消息关联序号候选** | `40850=0` 占 93.85%；非零值占 6.15%。关键发现：非零值中 99.88% 可在同一 `40027` 会话内匹配历史记录的 `40003`，目标不晚于当前记录；目标 `40003` 与 `40850` 的匹配率为 100%，而目标 `40005/40006/40008` 均无稳定匹配，因此 `40850` 更像直接引用目标消息的群内序号；与 `40851` 不共现，不能直接沿用 C2C 的 Reaction 语义 | — |
| `40851` | `INTEGER` | ❓ 低 | **未填充的关联时间字段候选** | 当前 `540,018` 行全部为 `0`；即使 `40850` 非零的 `33,219` 行中也全部为 `0`，暂无样本验证其是否为 Unix 时间戳、Reaction 时间或其他操作时间 | — |
| `40900` | `BLOB` | ✅ 高 | **引用/转发缓存 Protobuf** | 非 NULL 样本全部可由 group 专用超集解码；内部复用主表字段并嵌套 `40800/40801/40802` 快照 | [40900.md](40900.md) |

